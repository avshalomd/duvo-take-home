# App

> One line on what this is. **Live:** _deploying..._

## What it does

1. ...

## Run it locally

```bash
npm install
vercel env pull .env.local   # or set DATABASE_URL and one LLM key by hand, see .env.example
npm run db:push
npm run dev
```

Checks: `npm run check` (types, lint, unit tests) and `npm run test:e2e`.

## Architecture

## Decisions and trade-offs

## Not done yet, known issues, next steps
