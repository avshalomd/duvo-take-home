import type { Check, EvaluateInput } from "@/contracts/eval";

// placeholder: the tests land first and must fail on their assertions, not on a missing module.
export function runChecks(_input: EvaluateInput): Check[] {
  return [];
}
