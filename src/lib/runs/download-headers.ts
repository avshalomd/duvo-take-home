/** Not implemented yet: the tests in download-headers.test.ts come first. */
export function downloadHeaders(name: string, mime: string): Record<string, string> {
  void name;
  void mime;
  return {};
}
