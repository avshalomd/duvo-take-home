// The MCP server key a connection name becomes. Implemented in the feat commit that follows these tests.
export function connectionKey(name: string): string {
  void name;
  throw new Error("not implemented: connectionKey");
}
