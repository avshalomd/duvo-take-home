// /automations/<id>: the editor (while draft), the examples with their results, approval, history and schedule.
export default function AutomationPage() {
  return <main className="mx-auto w-full max-w-5xl flex-1 px-4 py-6 text-sm text-muted-foreground">The automation editor is coming.</main>; // STUB
}
