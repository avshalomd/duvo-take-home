// The workspace's pending invitations with Copy link and Revoke (QA Q109). The auth package builds it on its own
// members library; the Settings > Members page renders it under the member list.
export async function PendingInvitations(_props: { workspaceId: string; canManage: boolean }) {
  return null; // STUB: the auth package
}
